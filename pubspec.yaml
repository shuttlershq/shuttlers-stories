name: shuttlers_stories
description: A new Flutter package project.
version: 0.0.5
homepage: https://github.com/shuttlershq/shuttlers-stories.git

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  story_view: ^0.14.0
  screenshot: ^1.2.3
  share_plus: ^4.5.3
  path_provider: ^2.0.11

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:
